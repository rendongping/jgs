# F09 设计模式与软件工程基础

> 目标：掌握前端开发中常用的设计模式与软件工程原则，写出可维护、可扩展、可测试的代码。

---

## 核心要点（TL;DR）

- 设计模式是前人解决常见问题的经验总结，不是银弹。
- SOLID 原则是面向对象设计的基石，前端同样适用。
- 前端高频模式：单例、工厂、观察者、策略、装饰器、适配器、代理、模块模式。
- 设计模式的核心价值是降低耦合、提高复用、增强可测试性。
- 重构是持续改进代码结构的必要手段。

---

## 1. SOLID 原则

| 原则 | 含义 | 前端示例 |
|------|------|---------|
| SRP 单一职责 | 一个模块只做一件事 | 组件只负责展示，逻辑抽成 Hook |
| OCP 开闭原则 | 对扩展开放，对修改关闭 | 通过 props/slot 扩展组件 |
| LSP 里氏替换 | 子类可替换父类 | 抽象组件接口 |
| ISP 接口隔离 | 不依赖不需要的接口 | 拆分大而全的 props |
| DIP 依赖倒置 | 依赖抽象而非具体实现 | 依赖注入、接口驱动 |

---

## 2. 创建型模式

### 2.1 单例模式

确保一个类只有一个实例。

```js
class Singleton {
  static instance;
  constructor() {
    if (Singleton.instance) return Singleton.instance;
    Singleton.instance = this;
  }
}
```

### 2.2 工厂模式

封装对象创建逻辑。

```js
function createComponent(type) {
  const map = { button: Button, input: Input };
  return new map[type]();
}
```

### 2.3 建造者模式

分步骤构建复杂对象，如表单配置器。

---

## 3. 结构型模式

### 3.1 适配器模式

转换接口以兼容旧系统。

### 3.2 装饰器模式

在不修改原对象的情况下增强功能，如高阶组件 HOC。

### 3.3 代理模式

控制对象访问，如 Vue 响应式 Proxy、图片懒加载代理。

### 3.4 外观模式

提供统一接口隐藏复杂子系统，如统一 API 封装层。

---

## 4. 行为型模式

### 4.1 观察者模式

一对多依赖，状态变化通知订阅者。事件监听、Redux、Vue 响应式。

### 4.2 策略模式

封装一系列算法，使其可互相替换。如表单校验规则、主题切换。

### 4.3 命令模式

将请求封装为对象，支持撤销/重做。

### 4.4 迭代器模式

统一遍历接口，如 for...of、Generator。

### 4.5 状态模式

状态机管理复杂状态流转，如订单状态、表单步骤。

---

## 5. 前端特有模式

### 5.1 组件组合模式

组合优于继承，React/Vue 都推崇组合。

### 5.2 高阶组件（HOC）

复用组件逻辑。

### 5.3 Render Props / Slot

组件间共享渲染逻辑。

### 5.4 Hooks / Composables

逻辑复用的新范式。

### 5.5 容器/展示组件

分离业务逻辑与 UI 展示。

---

## 6. 重构技巧

- 提取函数/组件
- 消除重复代码
- 简化条件表达式
- 引入对象替代基本类型
- 拆分大类/大组件

---

## 常见误区

| 误区 | 正确理解 |
|------|---------|
| 过度设计 | 简单场景不需要复杂模式 |
| 生搬硬套 | 模式应服务于问题解决 |
| 忽视可测试性 | 设计时要考虑单元测试 |
| 模式越多越好 | 代码可读性优先 |

---

## 相关领域

- F01 JavaScript：语言特性实现模式。
- E04 Code Quality：测试与代码质量。
- A01 System Architecture：系统级设计原则。
- E06/E07 React/Vue：框架中的设计模式。

---

**标签**：`#design-patterns` `#solid` `#refactoring` `#software-engineering`

> **最后更新**：2026-06-25


---

## 本领域学习进度

<MarkComplete domainId="design-patterns" />
<ProgressTracker />
